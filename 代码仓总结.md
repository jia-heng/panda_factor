# PandaFactor 代码仓总结

## 项目概述

**PandaFactor** 是一个高性能量化因子库，用于金融数据分析、技术指标计算和因子构建，提供可视化图表支持。项目支持 Python 方式和公式方式编写因子，适合有编程基础和无编程基础的用户。

---

## 模块结构

```
panda_factor/
├── panda_common/       # 公共函数&工具类
├── panda_data          # 数据模块，提取数据与因子
├── panda_data_hub/     # 自动数据更新调度
├── panda_factor        # 因子计算与分析核心
├── panda_llm           # 大模型接入（支持OpenAI协议，兼容Deepseek）
├── panda_factor_server/ # 服务器API接口
├── panda_web/          # 前端Web页面（Vue）
└── requirements.txt    # 依赖列表
```

---

## 1. panda_common（公共基础模块）

提供全局配置、日志、数据库操作和数据模型。

### 主要文件

| 文件 | 功能 |
|------|------|
| `config.py` | 配置加载，支持从 `config.yaml` 和环境变量读取 |
| `config.yaml` | MongoDB 连接等基础配置 |
| `logger_config.py` | 日志配置 |
| `handlers/database_handler.py` | MongoDB 数据库操作封装 |
| `handlers/log_handler.py` | 日志处理 |
| `models/factor_analysis_params.py` | 因子分析参数模型 |
| `models/user_factor.py` | 用户因子数据模型 |
| `models/factor_analysis_log.py` | 因子分析日志模型 |
| `models/chart_data.py` | 图表数据模型 |
| `utils/stock_utils.py` | 股票相关工具函数 |
| `utils/globals.py` | 全局变量 |

---

## 2. panda_data（数据读取模块）

负责从 MongoDB 读取行情数据和因子数据。

### 主要文件

| 文件 | 功能 |
|------|------|
| `factor/factor_reader.py` | **核心数据读取类**，支持基础因子查询、自定义因子计算 |
| `market_data/market_data_reader.py` | 行情数据读取 |
| `market_data/market_stock_cn_minute_reader.py` | A股分钟线数据读取 |
| `market_data/partitioned_market_data_reader.py` | 分区行情数据读取 |
| `scripts/analyze_db_performance.py` | 数据库性能分析 |
| `scripts/benchmark_market_data.py` | 行情数据基准测试 |
| `scripts/create_indexes.py` | MongoDB 索引创建 |
| `scripts/optimize_mongodb.py` | MongoDB 优化脚本 |

### FactorReader 核心方法

- `get_factor()` — 获取基础量价因子（open/close/high/low/volume等）
- `get_custom_factor()` — 根据用户编写的公式/Python代码计算自定义因子
- `get_factor_by_name()` — 通过因子名称获取因子数据
- `get_custom_factor_competition()` — 因子大赛专用因子读取

---

## 3. panda_factor（因子计算核心模块）

因子构建、计算和分析的核心模块。

### 目录结构

```
panda_factor/
├── __init__.py
├── __main__.py           # CLI 入口
├── generate/            # 因子生成
│   ├── factor_base.py   # Factor 基类，定义 RANK/RETURNS/STDDEV 等算子
│   ├── factor_loader.py  # 因子加载器
│   ├── factor_wrapper.py # 因子包装器
│   ├── factor_utils.py  # 因子工具函数
│   ├── factor_data_handler.py
│   ├── factor_error_handler.py
│   ├── factor_constants.py
│   └── macro_factor.py  # 宏因子（支持公式和Python两种方式构建因子）
├── analysis/             # 因子分析
│   ├── factor_analysis.py # 因子分析主函数（分组回测、IC计算等）
│   ├── factor_analysis_workflow.py
│   ├── factor_ic_workflow.py
│   ├── factor_func.py   # 因子分析辅助函数
│   └── factor.py        # 因子分析结果类
├── data/                 # 数据处理
│   ├── data_provider.py
│   └── market_data_cleaner.py
├── models/
│   └── factor_params.py
└── utils/
    └── logger.py
```

### 因子编写方式

**Python 方式（继承 Factor 基类）：**

```python
class CustomFactor(Factor):
    def calculate(self, factors):
        close = factors['close']
        volume = factors['volume']
        returns = (close / DELAY(close, 20)) - 1
        volatility = STDDEV((close / DELAY(close, 1)) - 1, 20)
        momentum = RANK(returns)
        result = momentum * volatility
        return result
```

**公式方式：**

```python
RANK((CLOSE / DELAY(CLOSE, 20)) - 1) * STDDEV((CLOSE / DELAY(CLOSE, 1)) - 1, 20)
```

### 内置算子

`Factor` 基类提供以下算子方法：
- `RANK()` — 截面排名，归一化到 [-0.5, 0.5]
- `RETURNS()` — 收益率计算
- `STDDEV()` — 滚动标准差
- `CORRELATION()` — 滚动相关系数
- `IF()` — 条件选择
- `DELAY()` — 延迟/移位
- `SUM()` — 滚动求和

### 因子分析流程（factor_analysis.py）

1. 拉取 K 线数据（前复权处理）
2. 因子数据清洗（去极值 + Z-score 标准化）
3. 合并因子数据与市场数据
4. 计算滞后收益率
5. 分组回测（按因子值分组，计算各组收益）
6. 存库（分析结果写入 MongoDB）

---

## 4. panda_factor_server（API 服务模块）

基于 FastAPI 的因子分析 RESTful API 服务。

### 目录结构

```
panda_factor_server/
├── __init__.py
├── __main__.py
├── server.py              # FastAPI 应用入口
├── api/
├── routes/
│   └── user_factor_pro.py # 用户因子分析接口
├── services/
│   ├── user_factor_service.py
│   └── models/           # 请求/响应模型
│       ├── request_body.py
│       ├── response_body.py
│       ├── result_data.py
│       └── common.py
├── models/
│   ├── common.py
│   ├── request_body.py
│   ├── response_body.py
│   └── result_data.py
└── utils/
```

### 启动方式

```bash
python -m panda_factor_server
# 监听 0.0.0.0:8001
```

### 主要 API 路由

- `POST /api/v1/user_factor_pro` — 用户因子分析主接口

---

## 5. panda_llm（大模型接入模块）

基于 FastAPI 的 LLM 聊天服务，支持 OpenAI 协议，兼容 DeepSeek。

### 目录结构

```
panda_llm/
├── __init__.py
├── __main__.py
├── server.py              # FastAPI 应用入口
├── routes/
│   └── chat_router.py    # 聊天路由
├── services/
│   ├── chat_service.py   # 聊天服务
│   ├── llm_service.py     # LLM 服务（支持多模型）
│   ├── llm_service_bk.py  # LLM 服务备份
│   └── mongodb.py        # MongoDB 操作
└── models/
    └── chat.py           # 聊天数据模型
```

### 启动方式

```bash
python -m panda_llm
```

### 主要功能

- LLM 聊天接口
- DeepSeek 兼容
- OpenAI 协议兼容
- MongoDB 存储聊天记录

---

## 6. panda_web（前端页面模块）

基于 Vue.js 的 Web 前端，提供可视化界面。

### 主要文件

| 文件 | 功能 |
|------|------|
| `main.py` | FastAPI + StaticFiles 托管 Vue 构建产物 |

### 启动方式

```bash
python -m panda_web
# 监听 0.0.0.0:8080，重定向 / 到 /factor
```

---

## 7. panda_data_hub（数据更新调度模块）

负责从多个数据源（Tushare、RiceQuant、迅投 Xtquant）自动清洗和更新数据。

### 目录结构

```
panda_data_hub/
├── __init__.py
├── _main_auto_.py   # 自动调度入口（同时调度行情和因子清洗）
├── _main_clean_.py  # 手动清洗入口
├── data/            # 行情数据清洗
│   ├── tushare_stock_market_cleaner.py
│   ├── tushare_stocks_cleaner.py
│   ├── ricequant_stock_market_cleaner.py
│   ├── ricequant_stocks_cleaner.py
│   ├── xtquant_stock_market_cleaner.py
│   └── xtquant_stocks_cleaner.py
├── factor/          # 因子数据清洗
│   ├── ts_factor_clean_pro.py   # Tushare 因子清洗
│   ├── rq_factor_clean_pro.py   # RiceQuant 因子清洗
│   └── xt_factor_clean_pro.py   # Xtquant 因子清洗
├── routes/          # API 路由
│   ├── config/config_redefine.py
│   ├── data_clean/stock_market_data_clean.py
│   ├── data_clean/factor_data_clean.py
│   └── query/data_query.py
├── models/
│   ├── config_request.py
│   └── requestEntity.py
├── services/
│   ├── config/data_source_config_redefine_service.py
│   └── query/stock_statistic_service.py
└── task/
    ├── data_scheduler.py         # 行情数据调度器
    └── factor_clean_scheduler.py # 因子清洗调度器
```

### 支持的数据源

| 数据源 | 状态 |
|--------|------|
| Tushare | ✅ 已上线 |
| RiceQuant | ✅ 已上线 |
| 迅投 Xtquant | ✅ 已上线 |
| Tqsdk | 🔄 测试中 |
| QMT | 🔄 测试中 |
| Wind | 🔄 对接中 |
| Choice | 🔄 对接中 |

### 启动方式

```bash
# 自动调度（每日晚8点自动清洗）
python -m panda_data_hub._main_auto_
```

---

## 8. 项目依赖（requirements.txt）

主要依赖：
- `fastapi` / `uvicorn` — Web 框架
- `pandas` / `numpy` — 数据处理
- `pymongo` — MongoDB 驱动
- `pytask` / `tqdm` — 任务进度
- `pyyaml` — 配置文件解析
- `anthropic` / `openai` — LLM 接口

---

## 9. 项目架构图

```
                    ┌─────────────────┐
                    │   panda_web     │  (Vue 前端，端口 8080)
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │ panda_factor_   │
                    │    server       │  (FastAPI，端口 8001)
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
┌───────▼───────┐   ┌────────▼────────┐   ┌──────▼──────┐
│ panda_factor │   │   panda_llm     │   │panda_data_hub│
│  (核心因子)   │   │   (大模型)      │   │  (数据更新)  │
└───────┬───────┘   └─────────────────┘   └──────┬──────┘
        │                                         │
        │          ┌─────────────────┐            │
        │          │   panda_data    │◄───────────┘
        │          │   (数据读取)     │
        │          └────────┬────────┘
        │                   │
        └───────────────────┼─────────────────────┘
                            │
                   ┌────────▼────────┐
                   │  panda_common   │  (配置/日志/数据库)
                   └────────┬────────┘
                            │
                   ┌────────▼────────┐
                   │    MongoDB      │  (数据存储)
                   └─────────────────┘
```

---

## 10. 关键数据流

### 用户因子分析流程

1. **前端** 提交因子代码（Python 或公式）到 `panda_factor_server`
2. **FactorReader** 从 MongoDB 查询基础量价数据
3. **MacroFactor** 执行因子计算（公式解析 or Python 类执行）
4. **factor_analysis** 执行完整分析（去极值 → 标准化 → 分组回测 → IC计算）
5. 结果存入 MongoDB，前端展示回测/IC 图表

### 数据更新流程

1. **调度器** 每日定时触发（晚8点）
2. 从 Tushare/RiceQuant/Xtquant 拉取原始数据
3. **Cleaner** 清洗数据（对齐格式、处理缺失值）
4. 写入 MongoDB `stock_market` / `factor_base` 等集合

---

*文档生成时间：2026-04-09*
